package main;
import java.time.LocalDate;
import java.time.format.DateTimeFormatter;

import MySQL.MySQLAbonnementDAO;
import MySQL.MySQLClientDAO;
import MySQL.MySQLPeriodiciteDAO;
import MySQL.MySQLRevueDAO;
import POJO.Abonnement;
import POJO.Client;
import POJO.Periodicite;
import POJO.Revue;
import RequetesSQL.ReqAbonnement;

public class main {

	public static void main(String[] args) {
		
		
		// TODO Stub de la méthode généré automatiquement
		/*DateTimeFormatter formatage = DateTimeFormatter.ofPattern("dd/MM/yyyy");
		LocalDate dateDebut = LocalDate.parse("23/09/2019", formatage);
		LocalDate dateFin = LocalDate.parse("23/11/2019", formatage);
		
		MySQLClientDAO c = MySQLClientDAO.getInstance();
		Client Cl = new Client(1, "TOUBA", "Manon", "5", "les biscuits", "57000", "Metz", "France");
		
		MySQLPeriodiciteDAO p = MySQLPeriodiciteDAO.getInstance();
		Periodicite Per = new Periodicite(1, "CE SONT DES CHOSES");
		
		MySQLAbonnementDAO a = MySQLAbonnementDAO.getInstance();
		Abonnement Abo = new Abonnement(1, 2, LocalDate.of(2000, 02, 17), LocalDate.of(2003, 10, 26));
		
		MySQLRevueDAO r = MySQLRevueDAO.getInstance();
		Revue Rev = new Revue(1, "GFaim", "fiora la tchoin", 6.66, "jgfsjkd", 2);
		
		MySQLClientDAO C = MySQLClientDAO.getInstance();
		Client Cli = new Client(2, "MONGOLO", "Man", "5", "les biscuits c bon", "08600", "Givet", "France");
		
		MySQLPeriodiciteDAO P = MySQLPeriodiciteDAO.getInstance();
		Periodicite Peri = new Periodicite(2, "CE SONT DES CHOSES");
		
		MySQLAbonnementDAO A = MySQLAbonnementDAO.getInstance();
		Abonnement Abon = new Abonnement(2, 4, LocalDate.of(1900, 02, 17), LocalDate.of(1548, 10, 26));
		
		MySQLRevueDAO R = MySQLRevueDAO.getInstance();
		Revue Revu = new Revue(2, "GFaim oskour", "fiora la tchoin de ses morts", 6.66, "jgfsjkd", 2);
		
	*/
	}

	
}
